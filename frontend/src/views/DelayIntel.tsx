import type { LiveData } from '../hooks/useLiveData'

interface Props {
  liveData: LiveData
}

export function DelayIntel(_props: Props) {
  return <div className="label">DELAY INTEL — coming in Task 15</div>
}
